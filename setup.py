from setuptools import setup

setup(name='datastore',
      version='0.1',
      description='Store data as json from dict type access onto disk',
      url='http://google.com',
      author='Steven ODonnell',
      author_email='datastore@example.com',
      license='MIT',
      packages=['datastore'],
      zip_safe=False)
